package fr.formation.controllers;

import javax.ws.rs.Path;

@Path("Transfer")
public class TransferController {

    // private final static Map<String, transfer> TRANSFERTLIST = new
    // hashMap<>();
    static {
	// TRANSFERTLIST.put("ici des compte + Bban");
    }
    // @Get
    // @Path("/partiel1/{}")
    // @Produces(MediaType.APPLICATION_JSON)
}
